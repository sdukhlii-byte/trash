services:
  - type: web
    name: api
    runtime: node
    buildCommand: npm install && npm run build && npx prisma migrate deploy
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
